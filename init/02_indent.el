;----------------------------------------
; indentation for programming usage
;----------------------------------------
;; Note, however, that `C-q <TAB>' always inserts a tab character, regardless of the value of `indent-tabs-mode'.
(defun space-aholic()
  (setq indent-tabs-mode nil))
(setq tab-width 2)
(setq sgml-basic-offset 2)
(setq js-indent-level 2)
(setq css-indent-offset 2)
